# AI Quota 实施计划

1. 新增纯展示与轮换模型，先用测试锁定百分比钳制、窗口映射、缺失值和轮换行为。
2. 增加 AI Quota 首次运行默认值与精简设置页，复用现有 `SettingsStore` 和 Provider 登录流程。
3. 把合并状态栏接到方案 C 渲染器，按可用 Provider 每 8 秒轮换；数据更新后立即重绘。
4. 将合并菜单默认落在 Overview，复用现有方案 A 卡片，限制为 Codex / Claude 并保留刷新、设置、退出。
5. 将有历史数据时的过旧门槛改为连续 3 次失败，并验证成功后复位；配置 80% / 95% 已使用阈值。
6. 定制应用名、Bundle ID、默认中文和打包脚本，构建通用二进制与未签名 ZIP。
7. 运行新增聚焦测试、构建检查和全量检查；把上游 58 个既有问题与新增结果分开报告。
